import styled from 'tailwind';

const StyledWorkerProfile = styled.div`
  color: pink;
`;

export function WorkerProfile() {
  return (
    <StyledWorkerProfile>
      <h1>Welcome to WorkerProfile!</h1>
    </StyledWorkerProfile>
  );
}

export default WorkerProfile;
